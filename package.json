{
  "name": "gira-pointsmaxxer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "type": "module",
  "scripts": {
    "audit:high": "npm audit --audit-level=high",
    "dev": "vite dev",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "npm run build && node --test test/*.test.mjs",
    "test:browser": "npm run build && node --test test/browser-smoke.test.mjs",
    "test:server": "node --test test/server.test.mjs",
    "test:unit": "node --test test/planner.test.mjs",
    "serve": "node server.mjs",
    "start": "npm run build && node server.mjs"
  },
  "devDependencies": {
    "@eslint/js": "9.39.4",
    "eslint": "10.4.1",
    "globals": "17.6.0",
    "playwright-core": "1.60.0",
    "vite": "7.3.3"
  }
}
